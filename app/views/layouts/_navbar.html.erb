<nav class="navbar navbar-expand-lg navbar-dark">
	<a class="navbar-brand m-2 mr-3" href="<%= root_path %>">LE LOBBY</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse " id="navbarColor01">
		<ul class="navbar-nav mr-auto ml-3">
			<li class="nav-item active">
				<a class="nav-link text-white" href="<%= root_path %>">Commitments<span class="sr-only">(current)</span></a>
			</li>
    </ul>

		<ul class= "navbar-nav mr-1">
			<% if user_signed_in? %>
				<li class="nav-item">
					<%= link_to "Profil", user_path(current_user.id), class: 'nav-link text-white' %>
				</li>
				<% if isAdmin? %>
					<li class="nav-item">
						<%= link_to "Gestion des Mouvements", admin_commitments_path, class:'nav-link' %>
					</li>
					<li class="nav-item">
						<%= link_to "Gestion des Utilisateurs", admin_users_path, class:'nav-link' %>
					</li>
				<% end %>
				<li class="nav-item">
					<%= link_to "Déconnexion", destroy_user_session_path, method: 'delete', class: 'nav-link' %>
				</li>
			<% else %>
				<li class="nav-item">
					<%= link_to "Inscription", new_user_registration_path, class: 'nav-link text-white' %>
				</li>
				<li class="nav-item">
					<%= link_to "Connexion", new_user_session_path, class: 'nav-link text-white'  %> 
				</li>
			<% end %>

		</ul>
	</div>
</nav>


